# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in
this repository. It is deliberately a pointer and nothing else: everything that governs
work here is tool-agnostic, so it lives in AGENTS.md and is imported below rather than
kept in a second copy under a vendor's name.

@AGENTS.md
